# 🔧 إصلاح مشاكل النشر على Choreo

## ❌ المشاكل المكتشفة

### المشكلة 1: `BOT_TOKEN` غير موجود
```
ValueError: BOT_TOKEN غير موجود في متغيرات البيئة
```

### المشكلة 2: `sqlalchemy` غير مثبت
```
ModuleNotFoundError: No module named 'sqlalchemy'
ImportError: SQLAlchemyJobStore requires SQLAlchemy installed
```

---

## ✅ الحلول

### الحل 1: إضافة `sqlalchemy` إلى `requirements.txt`

تم إصلاح `requirements.txt` وإضافة:
```
SQLAlchemy>=2.0.0
```

**الآن يجب:**
1. رفع `requirements.txt` المحدث إلى GitHub
2. إعادة النشر على Choreo

---

### الحل 2: إضافة متغيرات البيئة في Choreo

في إعدادات Choreo، يجب إضافة متغيرات البيئة التالية:

#### متغيرات البيئة المطلوبة:

1. **`BOT_TOKEN`**
   - **القيمة:** `your_bot_token_here`
   - **مثال:** `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

2. **`ADMIN_IDS`** (مطلوب للأمان)
   - **القيمة:** `123456789,987654321`
   - **مثال:** معرفات الأدمين مفصولة بفواصل

3. **`DB_PATH`** (اختياري)
   - **القيمة:** `reminders.db`
   - **افتراضي:** `reminders.db`

4. **`BACKUP_DIR`** (اختياري)
   - **القيمة:** `backups`
   - **افتراضي:** `backups`

5. **`LOG_FILE`** (اختياري)
   - **القيمة:** `bot.log`
   - **افتراضي:** `bot.log`

---

## 📝 خطوات الإصلاح

### الخطوة 1: تحديث `requirements.txt`

✅ تم إصلاح `requirements.txt` - تم إضافة `SQLAlchemy>=2.0.0`

### الخطوة 2: رفع التغييرات إلى GitHub

```bash
cd "D:\Reminder bot"
git add requirements.txt
git commit -m "Add SQLAlchemy to requirements.txt"
git push origin main
```

### الخطوة 3: إضافة متغيرات البيئة في Choreo

1. **اذهب إلى Choreo Dashboard**
2. **افتح مشروعك (homework-bot)**
3. **اذهب إلى "Environment Variables" أو "Config"**
4. **أضف المتغيرات التالية:**

   ```
   BOT_TOKEN = your_bot_token_here
   ADMIN_IDS = 123456789,987654321
   ```

   **⚠️ مهم:**
   - لا تضع مسافات حول `=`
   - استبدل `your_bot_token_here` بـ BOT_TOKEN الحقيقي
   - استبدل `123456789,987654321` بمعرفات الأدمين الحقيقية

### الخطوة 4: إعادة النشر

1. **في Choreo Dashboard:**
   - اضغط "Redeploy" أو "Deploy"
   - انتظر حتى ينتهي Build

2. **تحقق من اللوغات:**
   - يجب أن تختفي أخطاء `BOT_TOKEN` و `sqlalchemy`
   - يجب أن يبدأ البوت بنجاح

---

## 🔍 التحقق من النجاح

بعد إعادة النشر، تحقق من:

1. **لا توجد أخطاء `BOT_TOKEN`**
2. **لا توجد أخطاء `sqlalchemy`**
3. **البوت يبدأ بنجاح**
4. **يمكنك إرسال `/start` للبوت**

---

## 📋 قائمة متغيرات البيئة الكاملة

| المتغير | مطلوب | الوصف | مثال |
|---------|-------|-------|------|
| `BOT_TOKEN` | ✅ نعم | رمز البوت من BotFather | `1234567890:ABC...` |
| `ADMIN_IDS` | ✅ نعم | معرفات الأدمين (مفصولة بفواصل) | `123456789,987654321` |
| `DB_PATH` | ❌ لا | مسار قاعدة البيانات | `reminders.db` |
| `BACKUP_DIR` | ❌ لا | مجلد النسخ الاحتياطي | `backups` |
| `LOG_FILE` | ❌ لا | ملف اللوغات | `bot.log` |

---

## ⚠️ ملاحظات مهمة

1. **لا ترفع ملف `.env` إلى GitHub** - يحتوي على `BOT_TOKEN`
2. **استخدم متغيرات البيئة في Choreo** - أكثر أماناً
3. **تحقق من `requirements.txt`** - يجب أن يحتوي على `SQLAlchemy`
4. **راقب اللوغات** - للتأكد من نجاح النشر

---

## 🐛 إذا استمرت المشاكل

### المشكلة: لا يزال `sqlalchemy` غير موجود

**الحل:**
1. تحقق من أن `requirements.txt` يحتوي على `SQLAlchemy>=2.0.0`
2. تحقق من أنك رفعت التغييرات إلى GitHub
3. تحقق من أن Choreo يعيد بناء المشروع من GitHub

### المشكلة: لا يزال `BOT_TOKEN` غير موجود

**الحل:**
1. تحقق من أنك أضفت `BOT_TOKEN` في Environment Variables
2. تحقق من أن القيمة صحيحة (بدون مسافات)
3. تحقق من أنك حفظت التغييرات
4. أعد النشر

---

## ✅ بعد الإصلاح

بعد إصلاح المشاكل:
1. ✅ البوت يعمل على Choreo
2. ✅ يمكنك إرسال `/start` للبوت
3. ✅ جميع الميزات تعمل

---

## 📚 للمزيد من المساعدة

- راجع `RENDER_DEPLOY_GUIDE.md` (نفس المبدأ)
- راجع `README_DEPLOY.md` للدليل الشامل

