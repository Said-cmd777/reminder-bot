# دليل رفع الملفات إلى GitHub (خطوة بخطوة)

## الخطوة 1: تثبيت Git

### التحقق من تثبيت Git

افتح **Terminal** (PowerShell أو CMD) واكتب:
```bash
git --version
```

إذا ظهر رقم الإصدار، Git مثبت ✅
إذا ظهر خطأ، يجب تثبيت Git أولاً.

### تثبيت Git (إذا لم يكن مثبتاً)

1. اذهب إلى https://git-scm.com/download/win
2. حمّل Git for Windows
3. شغّل الملف المثبت
4. اضغط "Next" في جميع الخطوات (الإعدادات الافتراضية جيدة)
5. بعد التثبيت، أعد فتح Terminal

---

## الخطوة 2: إنشاء حساب GitHub

1. اذهب إلى https://github.com
2. اضغط "Sign up"
3. أدخل:
   - **Username** (اسم المستخدم)
   - **Email** (البريد الإلكتروني)
   - **Password** (كلمة المرور)
4. اضغط "Create account"
5. تحقق من بريدك الإلكتروني

---

## الخطوة 3: إنشاء Repository جديد على GitHub

1. بعد تسجيل الدخول، اضغط على **"+"** في الأعلى
2. اختر **"New repository"**
3. أدخل:
   - **Repository name:** `telegram-bot` (أو أي اسم تريده)
   - **Description:** `Telegram Bot for Reminders` (اختياري)
   - **Public** أو **Private** (اختيارك)
4. **⚠️ مهم:** لا تضع علامة ✓ على "Add a README file"
5. اضغط **"Create repository"**

---

## الخطوة 4: رفع الملفات من الكمبيوتر إلى GitHub

### الطريقة 1: استخدام Terminal (الأسهل)

1. **افتح Terminal (PowerShell أو CMD)**
   - اضغط `Win + R`
   - اكتب `powershell` أو `cmd`
   - اضغط Enter

2. **انتقل إلى مجلد المشروع:**
   ```bash
   cd "D:\Reminder bot"
   ```

3. **تهيئة Git:**
   ```bash
   git init
   ```
   - سترى رسالة: `Initialized empty Git repository`

4. **إضافة جميع الملفات:**
   ```bash
   git add .
   ```
   - هذا يضيف جميع الملفات إلى Git

5. **حفظ التغييرات (Commit):**
   ```bash
   git commit -m "Initial commit"
   ```
   - هذا يحفظ الملفات في Git

6. **ربط المشروع بـ GitHub:**
   ```bash
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   ```
   - **استبدل:**
     - `YOUR_USERNAME` → اسم المستخدم على GitHub
     - `YOUR_REPO_NAME` → اسم Repository الذي أنشأته
   - **مثال:**
     ```bash
     git remote add origin https://github.com/khaled/telegram-bot.git
     ```

7. **رفع الملفات إلى GitHub:**
   ```bash
   git push -u origin main
   ```
   - سيطلب منك اسم المستخدم وكلمة المرور
   - أدخل اسم المستخدم وكلمة المرور على GitHub
   - إذا ظهرت رسالة خطأ حول كلمة المرور، اقرأ "الطريقة 2" أدناه

---

### الطريقة 2: استخدام GitHub Desktop (أسهل للمبتدئين)

1. **تحميل GitHub Desktop:**
   - اذهب إلى https://desktop.github.com
   - حمّل GitHub Desktop
   - شغّله وثبّته

2. **تسجيل الدخول:**
   - سجّل دخول بحساب GitHub

3. **إضافة المشروع:**
   - اضغط "File" → "Add local repository"
   - اختر مجلد `D:\Reminder bot`
   - اضغط "Add repository"

4. **رفع الملفات:**
   - اكتب رسالة: `Initial commit`
   - اضغط "Commit to main"
   - اضغط "Publish repository"
   - اختر Repository من القائمة (أو أنشئ واحد جديد)
   - اضغط "Publish repository"

---

### الطريقة 3: استخدام GitHub Website (بدون Terminal)

1. **إنشاء Repository على GitHub:**
   - اذهب إلى https://github.com/new
   - أدخل اسم Repository
   - اضغط "Create repository"

2. **رفع الملفات عبر الموقع:**
   - بعد إنشاء Repository، سترى صفحة بها أوامر
   - **لكن الأسهل:**
     - اضغط "uploading an existing file"
     - اسحب الملفات من مجلد `D:\Reminder bot` إلى الصفحة
     - اضغط "Commit changes"

---

## ⚠️ حل المشاكل الشائعة

### المشكلة 1: Git يطلب اسم المستخدم وكلمة المرور

**الحل:**
1. استخدم **Personal Access Token** بدلاً من كلمة المرور:
   - اذهب إلى https://github.com/settings/tokens
   - اضغط "Generate new token (classic)"
   - اختر الصلاحيات: `repo` (جميع الصلاحيات)
   - اضغط "Generate token"
   - انسخ الرمز (Token)
   - استخدمه ككلمة مرور عند `git push`

### المشكلة 2: "fatal: not a git repository"

**الحل:**
- تأكد من أنك في مجلد المشروع الصحيح
- شغّل `git init` أولاً

### المشكلة 3: "fatal: remote origin already exists"

**الحل:**
- احذف الـ remote القديم:
  ```bash
  git remote remove origin
  ```
- أضف الـ remote الجديد:
  ```bash
  git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
  ```

### المشكلة 4: "error: failed to push some refs"

**الحل:**
- سحب التغييرات من GitHub أولاً:
  ```bash
  git pull origin main --allow-unrelated-histories
  ```
- ثم ارفع الملفات:
  ```bash
  git push -u origin main
  ```

---

## ✅ التحقق من نجاح الرفع

1. اذهب إلى https://github.com/YOUR_USERNAME/YOUR_REPO_NAME
2. تحقق من أن جميع الملفات موجودة
3. يجب أن ترى:
   - `bot.py`
   - `handlers.py`
   - `requirements.txt`
   - `Procfile`
   - وغيرها من الملفات

---

## 📝 ملخص الأوامر السريعة

```bash
# 1. الانتقال إلى مجلد المشروع
cd "D:\Reminder bot"

# 2. تهيئة Git
git init

# 3. إضافة جميع الملفات
git add .

# 4. حفظ التغييرات
git commit -m "Initial commit"

# 5. تغيير اسم الفرع
git branch -M main

# 6. ربط المشروع بـ GitHub
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 7. رفع الملفات
git push -u origin main
```

---

## 🎯 الخطوات التالية

بعد رفع الملفات إلى GitHub:

1. **اذهب إلى Render.com:**
   - https://render.com
   - سجّل دخول بحساب GitHub

2. **انشر البوت:**
   - راجع `RENDER_DEPLOY_GUIDE.md`

---

## 💡 نصائح

1. **استخدم GitHub Desktop** إذا كنت مبتدئاً
2. **احفظ Personal Access Token** في مكان آمن
3. **لا ترفع ملف `.env`** (يحتوي على `BOT_TOKEN`)
4. **تحقق من `.gitignore`** قبل الرفع

---

## للمساعدة

إذا واجهت مشاكل:
1. تحقق من أن Git مثبت
2. تحقق من أنك في المجلد الصحيح
3. تحقق من اسم المستخدم واسم Repository
4. راجع الأخطاء في Terminal

---

## مثال كامل

```bash
# 1. الانتقال إلى مجلد المشروع
cd "D:\Reminder bot"

# 2. تهيئة Git
git init

# 3. إضافة جميع الملفات
git add .

# 4. حفظ التغييرات
git commit -m "Initial commit"

# 5. تغيير اسم الفرع
git branch -M main

# 6. ربط المشروع بـ GitHub (استبدل YOUR_USERNAME و YOUR_REPO_NAME)
git remote add origin https://github.com/khaled/telegram-bot.git

# 7. رفع الملفات (سيطلب اسم المستخدم وكلمة المرور)
git push -u origin main
```

---

## ✅ بعد الرفع

1. تحقق من GitHub: https://github.com/YOUR_USERNAME/YOUR_REPO_NAME
2. تأكد من وجود جميع الملفات
3. اذهب إلى Render.com لنشر البوت

---

## 🎉 تم!

الآن ملفاتك على GitHub ويمكنك نشر البوت على Render.com!

