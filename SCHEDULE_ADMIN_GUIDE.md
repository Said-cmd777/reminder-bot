# دليل إدارة الجداول الأسبوعية

## 📋 نظرة عامة

تم إنشاء نظام إدارة شامل للجداول الأسبوعية يسمح للأدمن بتعديل وإدارة الجداول دون الحاجة لتعديل الكود.

## 🏗️ البنية المعمارية

### 1. قاعدة البيانات (Database)
تم إنشاء 3 جداول جديدة:
- **weekly_schedule_classes**: تخزين الحصص
- **schedule_locations**: تخزين مواقع الحجرات مع روابط Google Maps
- **alternating_weeks_config**: إدارة الحصص الدورية (الأسبوع المرجعي)

### 2. ملفات جديدة
- **db_schedule.py**: دوال قاعدة البيانات لإدارة الجداول
- **bot_handlers/schedule_admin_helpers.py**: دوال مساعدة للواجهة الإدارية
- **init_schedule_db.py**: سكريبت تهيئة قاعدة البيانات بالبيانات الحالية

### 3. التكامل مع النظام الحالي
- **weekly_schedule.py**: تم تحديثه لقراءة من قاعدة البيانات أولاً، مع fallback للبيانات الثابتة
- **handlers.py**: إضافة معالجات إدارية كاملة
- **bot_handlers/helpers.py**: إضافة زر "إدارة الجداول الأسبوعية" في القائمة الرئيسية

## 🎯 الميزات

### 1. عرض الجداول
- عرض جميع المجموعات
- عرض الجدول الكامل لمجموعة
- عرض حصص يوم محدد

### 2. إضافة حصة
خطوات إضافة حصة جديدة:
1. اختيار المجموعة
2. اختيار اليوم
3. إدخال وقت البداية (مثال: 08:00)
4. إدخال وقت الانتهاء (مثال: 09:30)
5. إدخال اسم المادة
6. إدخال المكان
7. اختيار نوع الحصة (Course, Tutorial Session, Laboratory Session, Online Session)
8. تحديد إذا كانت الحصة دورية
9. إذا كانت دورية: إدخال مفتاح الحصة الدورية (مثال: algorithm1)

### 3. تعديل حصة
- عرض تفاصيل الحصة
- إمكانية الحذف (سيتم إضافة التعديل لاحقاً)

### 4. حذف حصة
- تأكيد قبل الحذف
- تحديث الجدول تلقائياً

### 5. إدارة الحصص الدورية
- حفظ تاريخ مرجعي لكل حصة دورية
- حساب تلقائي للأسبوع الحالي
- عرض/تعديل إعدادات الحصص الدورية (سيتم إضافتها لاحقاً)

### 6. إدارة المواقع
- عرض/تعديل مواقع الحجرات (سيتم إضافتها لاحقاً)
- ربط تلقائي مع Google Maps

## 🚀 الاستخدام

### 1. تهيئة قاعدة البيانات
```bash
python init_schedule_db.py
```
هذا الأمر يحمل البيانات الحالية من `weekly_schedule.py` إلى قاعدة البيانات.

### 2. الوصول للواجهة الإدارية
1. افتح البوت
2. اذهب إلى "Homeworks"
3. اضغط على "⚙️ إدارة الجداول الأسبوعية" (للأدمن فقط)
4. اختر المجموعة
5. اختر اليوم
6. استخدم الخيارات المتاحة

## 🔧 التصميم الذكي

### 1. Fallback Mechanism
- النظام يقرأ من قاعدة البيانات أولاً
- إذا فشل، يعود للبيانات الثابتة في `weekly_schedule.py`
- يضمن عدم توقف النظام في حالة وجود مشاكل

### 2. Thread Safety
- استخدام locks لحماية البيانات من race conditions
- معالجة حالات متعددة للمستخدمين في نفس الوقت

### 3. Validation
- التحقق من صحة البيانات المدخلة
- رسائل خطأ واضحة
- إمكانية الإلغاء في أي خطوة

### 4. User Experience
- واجهة سهلة ومباشرة
- رسائل واضحة
- تأكيد قبل الحذف
- إمكانية الرجوع في أي وقت

## 📝 ملاحظات مهمة

1. **الحصص الدورية**: النظام يحسب تلقائياً الأسبوع الحالي بناءً على التاريخ المرجعي
2. **المواقع**: يجب إضافة المواقع إلى قاعدة البيانات أولاً (تم ذلك تلقائياً في init_schedule_db.py)
3. **التعديل**: حالياً يمكن إضافة وحذف الحصص فقط (التعديل سيتم إضافته لاحقاً)
4. **المجموعات الجديدة**: يمكن إضافة مجموعات جديدة من الواجهة (سيتم إضافتها لاحقاً)

## 🔮 التحسينات المستقبلية

1. إضافة ميزة تعديل الحصص (بدلاً من الحذف والإضافة)
2. إدارة المواقع من الواجهة
3. إدارة الحصص الدورية من الواجهة
4. إضافة مجموعات جديدة من الواجهة
5. تصدير/استيراد الجداول
6. نسخ جدول من مجموعة إلى أخرى

