# نشر سريع على Render.com (5 دقائق)

## الخطوات السريعة

### 1. رفع الكود إلى GitHub (دقيقة واحدة)

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

### 2. إنشاء حساب Render (دقيقة واحدة)

1. اذهب إلى https://render.com
2. سجّل بحساب GitHub
3. اضغط "New +" → "Web Service"

### 3. إعداد البوت (دقيقتان)

1. **اختر Repository:**
   - اختر Repository الخاص بك

2. **الإعدادات:**
   - **Name:** telegram-bot
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python run_bot_service.py`
   - **Plan:** Free

3. **Environment Variables:**
   - اضغط "Advanced" → "Add Environment Variable"
   - أضف:
     ```
     BOT_TOKEN = your_bot_token_here
     ADMIN_IDS = 123456789,987654321
     ```

### 4. النشر (دقيقة واحدة)

1. اضغط "Create Web Service"
2. انتظر حتى ينتهي Build (2-3 دقائق)
3. تحقق من اللوغات
4. اختبر البوت بـ `/start`

---

## ✅ تم! البوت يعمل الآن 24/7 مجاناً!

---

## ملاحظات مهمة

### ⚠️ قاعدة البيانات

Render.com يحذف الملفات عند إعادة النشر. استخدم:

**Option 1: قاعدة بيانات خارجية (مُوصى به)**
- استخدم PostgreSQL على Render (مجاني)
- أو استخدم SQLite مع نسخ احتياطي يومي

**Option 2: Render Disk (مدفوع)**
- يستمر بعد إعادة النشر
- لكن غير مجاني

### 🔄 التحديثات

- كل push جديد إلى GitHub سيُحدث البوت تلقائياً
- لا حاجة لإعادة النشر يدوياً

### 📊 المراقبة

- راقب اللوغات في Render Dashboard
- تحقق من الأخطاء بشكل دوري

---

## استكشاف الأخطاء

### البوت لا يعمل؟
1. تحقق من اللوغات
2. تحقق من `BOT_TOKEN`
3. تحقق من `Start Command`

### قاعدة البيانات مفقودة؟
1. استخدم PostgreSQL
2. أو استخدم نسخ احتياطي تلقائي

---

## للمزيد من التفاصيل

راجع `README_DEPLOY.md` للدليل الشامل.

