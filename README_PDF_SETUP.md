# 📄 كيفية إضافة ملفات PDF للجداول الأسبوعية

## ✅ تم إعداد النظام

تم إضافة دعم كامل لإرسال ملفات PDF للجداول الأسبوعية.

## 📁 مكان الملفات

ضع ملفات PDF في المجلد التالي:
```
D:\Reminder bot\schedules\
```

## 📝 تسمية الملفات

### للمجموعات المحددة:
- `weekly_schedule_group_01.pdf` - للمجموعة 01
- `weekly_schedule_group_02.pdf` - للمجموعة 02
- `weekly_schedule_group_03.pdf` - للمجموعة 03
- `weekly_schedule_group_04.pdf` - للمجموعة 04

### ملف عام (اختياري):
- `weekly_schedule_all.pdf` - يُستخدم إذا لم يُوجد ملف خاص بالمجموعة

## 🚀 كيفية الاستخدام

1. **ضع ملف PDF في المجلد:**
   - انسخ ملف PDF إلى `D:\Reminder bot\schedules\`
   - أعد تسميته حسب التسمية المطلوبة (مثلاً: `weekly_schedule_group_01.pdf`)

2. **اختبر البوت:**
   - شغّل البوت
   - اضغط على "Weekly Schedule" → اختر مجموعة → "التوقيت الأسبوعي كاملا"
   - سيتم إرسال الجدول النصي أولاً، ثم ملف PDF تلقائياً

## 📌 ملاحظات

- إذا لم يُوجد ملف PDF، سيتم إرسال الجدول النصي فقط (بدون خطأ)
- الملفات يجب أن تكون بصيغة PDF
- تأكد من أن البوت لديه صلاحية القراءة من مجلد `schedules/`

## 🔍 التحقق من الملفات

يمكنك التحقق من الملفات الموجودة:

```bash
dir schedules
```

أو في Python:

```python
import os
from config import SCHEDULES_DIR

if SCHEDULES_DIR and os.path.exists(SCHEDULES_DIR):
    files = os.listdir(SCHEDULES_DIR)
    pdf_files = [f for f in files if f.endswith('.pdf')]
    print(f"PDF files: {pdf_files}")
```

## ❓ حل المشاكل

### المشكلة: لا يتم إرسال PDF
**الحل:**
1. تأكد من أن الملف موجود في المجلد `schedules/`
2. تأكد من أن اسم الملف صحيح (مثلاً: `weekly_schedule_group_01.pdf`)
3. تحقق من اللوغ (`bot.log`) لمعرفة الأخطاء

### المشكلة: خطأ في القراءة
**الحل:**
1. تأكد من أن البوت لديه صلاحية القراءة
2. تأكد من أن الملف ليس مفتوحاً في برنامج آخر
3. تأكد من أن الملف صحيح (ليس تالفاً)

## 📞 الدعم

إذا واجهت أي مشكلة، راجع ملف `bot.log` للتفاصيل.


