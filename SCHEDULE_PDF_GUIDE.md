# 📄 دليل ملفات PDF للجداول الأسبوعية

## 📁 مجلد الجداول

يتم تخزين ملفات PDF للجداول الأسبوعية في المجلد `schedules/`.

## 📝 تسمية الملفات

### للمجموعات المحددة:
- `weekly_schedule_group_01.pdf` - للجدول الأسبوعي للمجموعة 01
- `weekly_schedule_group_02.pdf` - للجدول الأسبوعي للمجموعة 02
- `weekly_schedule_group_03.pdf` - للجدول الأسبوعي للمجموعة 03
- `weekly_schedule_group_04.pdf` - للجدول الأسبوعي للمجموعة 04

### ملف عام (اختياري):
- `weekly_schedule_all.pdf` - يتم إرساله إذا لم يُوجد ملف خاص بالمجموعة

## 🚀 كيفية الاستخدام

1. **ضع ملف PDF في مجلد `schedules/`:**
   ```bash
   schedules/
   ├── weekly_schedule_group_01.pdf
   ├── weekly_schedule_group_02.pdf
   ├── weekly_schedule_group_03.pdf
   └── weekly_schedule_group_04.pdf
   ```

2. **عندما يضغط المستخدم على "التوقيت الأسبوعي كاملا":**
   - سيتم إرسال الجدول النصي أولاً
   - ثم سيتم إرسال ملف PDF تلقائياً (إذا كان موجوداً)

## ⚙️ الإعدادات

يمكنك تغيير مجلد الجداول من خلال متغير البيئة:

```bash
# في ملف .env
SCHEDULES_DIR=schedules
```

أو في متغيرات البيئة:

```bash
export SCHEDULES_DIR=schedules
```

## 📌 ملاحظات

- إذا لم يُوجد ملف PDF للمجموعة المحددة، سيتم البحث عن `weekly_schedule_all.pdf`
- إذا لم يُوجد أي ملف PDF، سيتم إرسال الجدول النصي فقط (بدون خطأ)
- الملفات يجب أن تكون بصيغة PDF
- تأكد من أن البوت لديه صلاحية القراءة من مجلد `schedules/`

## 🔍 التحقق من الملفات

يمكنك التحقق من الملفات الموجودة:

```python
import os
from config import SCHEDULES_DIR

if SCHEDULES_DIR and os.path.exists(SCHEDULES_DIR):
    files = os.listdir(SCHEDULES_DIR)
    pdf_files = [f for f in files if f.endswith('.pdf')]
    print(f"PDF files found: {pdf_files}")
```

## 🎯 مثال

عندما يضغط مستخدم Group 01 على "التوقيت الأسبوعي كاملا":
1. يتم إرسال الجدول النصي
2. يتم البحث عن `weekly_schedule_group_01.pdf`
3. إذا وُجد، يتم إرساله
4. إذا لم يُوجد، يتم البحث عن `weekly_schedule_all.pdf`
5. إذا وُجد، يتم إرساله
6. إذا لم يُوجد أي ملف، يتم إرسال الجدول النصي فقط


