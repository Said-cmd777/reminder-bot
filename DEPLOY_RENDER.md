# نشر البوت على Render.com (مجاني 24/7) 🚀

## الخطوات السريعة (5 دقائق)

### 1️⃣ رفع الكود إلى GitHub

```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

### 2️⃣ إنشاء حساب Render.com

1. اذهب إلى https://render.com
2. اضغط "Get Started for Free"
3. سجّل بحساب GitHub (أسهل طريقة)

### 3️⃣ نشر البوت

1. **في Render Dashboard:**
   - اضغط "New +" → "Background Worker" (أو "Web Service")
   - اختر "Build and deploy from a Git repository"
   - اختر Repository الخاص بك

2. **الإعدادات:**
   - **Name:** `telegram-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python run_bot_service.py`
   - **Plan:** `Free`

3. **Environment Variables:**
   - اضغط "Advanced" → "Add Environment Variable"
   - أضف:
     ```
     BOT_TOKEN = your_bot_token_here
     ADMIN_IDS = 123456789,987654321
     ```
   - **⚠️ مهم:** لا تضع مسافات حول `=`

4. **النشر:**
   - اضغط "Create Background Worker"
   - انتظر حتى ينتهي Build (2-3 دقائق)
   - تحقق من اللوغات

### 4️⃣ اختبار البوت

1. افتح Telegram
2. أرسل `/start` للبوت
3. تحقق من أن البوت يرد ✅

---

## ✅ تم! البوت يعمل الآن 24/7 مجاناً!

---

## ⚠️ ملاحظات مهمة

### قاعدة البيانات

**المشكلة:** Render.com يحذف الملفات عند إعادة النشر.

**الحلول:**

#### Option 1: قاعدة بيانات خارجية (مُوصى به) ⭐

استخدم **PostgreSQL على Render** (مجاني):

1. **في Render Dashboard:**
   - اضغط "New +" → "PostgreSQL"
   - اختر "Free" plan
   - انسخ "Internal Database URL"

2. **إضافة Environment Variable:**
   ```
   DATABASE_URL = postgresql://user:pass@host:port/dbname
   ```

3. **تعديل الكود:**
   - غير `db.py` لاستخدام PostgreSQL بدلاً من SQLite
   - أو استخدم SQLAlchemy مع PostgreSQL

#### Option 2: Render Disk (مدفوع)

- يستمر بعد إعادة النشر
- لكن غير مجاني ($5/شهر)

#### Option 3: نسخ احتياطي تلقائي

- استخدم GitHub Actions لنشر نسخ احتياطي يومي
- أو استخدم Dropbox/Google Drive API

---

## 🔄 التحديثات التلقائية

- كل push جديد إلى GitHub سيُحدث البوت تلقائياً
- لا حاجة لإعادة النشر يدوياً
- فعّل "Auto-Deploy" في Render Settings

---

## 📊 المراقبة

### اللوغات

- اضغط "Logs" في Render Dashboard
- راقب الأخطاء بشكل دوري
- تحقق من أن البوت يعمل بدون مشاكل

### الحالة

- اضغط "Metrics" لمراقبة استخدام الموارد
- تحقق من أن البوت لا يستخدم موارد كثيرة

---

## 🐛 استكشاف الأخطاء

### البوت لا يعمل؟

1. **تحقق من اللوغات:**
   - افتح "Logs" في Render Dashboard
   - ابحث عن أخطاء

2. **تحقق من Environment Variables:**
   - تأكد من أن `BOT_TOKEN` صحيح
   - تأكد من أن `ADMIN_IDS` صحيح

3. **تحقق من Start Command:**
   - يجب أن يكون: `python run_bot_service.py`
   - تأكد من وجود `run_bot_service.py` في المشروع

### قاعدة البيانات مفقودة؟

1. **استخدم PostgreSQL:**
   - أنشئ PostgreSQL database على Render
   - أضف `DATABASE_URL` إلى Environment Variables
   - عدّل الكود لاستخدام PostgreSQL

2. **أو استخدم نسخ احتياطي:**
   - أضف نسخ احتياطي تلقائي إلى GitHub
   - استخدم GitHub Actions

### البوت يتوقف؟

1. **تحقق من إعدادات إعادة التشغيل:**
   - تأكد من أن `run_bot_service.py` يعمل
   - تحقق من اللوغات

2. **تحقق من استخدام الموارد:**
   - Render Free plan محدود
   - راقب استخدام RAM و CPU

---

## 💡 نصائح

1. **استخدم PostgreSQL:**
   - أفضل من SQLite للاستضافة السحابية
   - مجاني على Render

2. **راقب اللوغات:**
   - تحقق من اللوغات يومياً
   - ابحث عن أخطاء

3. **نسخ احتياطي:**
   - قم بنسخ احتياطي يومي
   - استخدم GitHub Actions

4. **التحديثات:**
   - فعّل Auto-Deploy
   - كل push جديد سيُحدث البوت تلقائياً

---

## 📚 روابط مفيدة

- **Render.com:** https://render.com
- **Render Docs:** https://render.com/docs
- **Render Python Guide:** https://render.com/docs/deploy-python

---

## 🎉 الخلاصة

**Render.com هو أفضل خيار مجاني للنشر:**
- ✅ مجاني تماماً
- ✅ يعمل 24/7
- ✅ سهل الاستخدام
- ✅ تحديثات تلقائية
- ✅ SSL تلقائي

**استخدم PostgreSQL لقاعدة البيانات:**
- ✅ مجاني
- ✅ موثوق
- ✅ يستمر بعد إعادة النشر

---

## للمساعدة

إذا واجهت مشاكل:
1. تحقق من اللوغات في Render Dashboard
2. تحقق من Environment Variables
3. راجع `README_DEPLOY.md` للدليل الشامل
4. تحقق من أن جميع الملفات موجودة

